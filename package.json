{
  "name": "dwandwa",
  "private": true,
  "scripts": {
    "predev": "node scripts/generate-hosted-fonts.mjs",
    "dev": "vite --host",
    "prebuild": "node scripts/generate-hosted-fonts.mjs",
    "build": "vite build",
    "build:github-pages": "DWANDWA_BASE=github vite build",
    "preview": "vite preview --host",
    "smoke": "node scripts/smoke-check.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-pwa": "^0.21.2"
  },
  "dependencies": {
    "three": "^0.160.0"
  }
}
